# 2026-09-25: PR #18 (定義域の宣言と生成器によるテスト) の振り返り

読者: このリポで次に PR を回す実装セッション (main agent)。前提知識は AGENTS.md と review-perspectives。

## 状況

- PR #15 は 78 スレッド・約 50 ラウンドの点修正の連鎖だった。PR #18 はそれを止めるために、定義域の宣言・canon の目録・生成器のテスト・findings.md の回数規則を入れた。
- PR #18 自身が Codex から 19 ラウンド、ローカルレビュー 6 回を受けた。

## 良かったこと

- 定義域を先に閉じる順序が機能した。curl の proxy の指摘 (P1) に対し、`--noproxy` の点修正でなく、curl が読む入力の目録 (canon `facts/curl/environment-and-config-inputs`) を先に書き、`-q` と loopback 限定の `--noproxy` で閉じ、テストは子プロセスの環境を全部握って回す形にした。
- 3 回目の指摘で機構を作り直す規則が働いた。`core.hooksPath` の段は順序 → scope → command scope と 3 回指摘され、3 回目で「永続化された値を command scope を外して読む」に作り直し、出どころの目録を canon に置いた (`facts/git/config-sources-and-scopes`)。
- 実装 subagent が指示を実測で覆した。「`--noproxy` を無条件で渡す」という指示に対し、`--noproxy` が `NO_PROXY` を置き換えることを実測し、127.0.0.1 宛てだけに限る形にしてテストで固定した。指示より契約を優先する動きが望ましい。
- 観点の問いに対して反証する規則 (救済案でなく主張を反証する) で、コメント担当の誤指摘を機械的に却下できた。

## 直したこと

| 問題 | 直した先 |
| --- | --- |
| 次元を閉じる場所を間違えた。proxy の指摘を test の helper (観測した側) で閉じ、製品の curl (次元に触れる操作) を守っていなかった | AGENTS.md に「次元はそれに触れる操作の側で閉じる」を足した |
| findings.md を push で消していたので、外部レビューが続ける同じ箇所への指摘の回数が 1 に戻っていた | 系列の終わり (PR の merge か close) まで残し、外部レビューの確定した指摘も書く (`3d9b04a`) |
| verify.sh に追記で育てた結果、hook を有効にする設定が検査の後ろに来て、検査が落ちると hook が無いままだった | その場で直す設定を検査より先に置き、fresh clone で検査が落ちても hook が入る fixture (`2c15d96`, `5960dd1`)。verify.sh の先頭に段の順序を書いた |
| watch が Codex の利用上限のコメントを黙って飲み、25 分待って何も得なかった | watch が上限の行を出し、窓の reset を app-server から読んで過ぎたら `@codex review` を出す (`17dec46`, `2a98011`) |
| canon の「上限からの自動再開」は時系列の読み違いだった (回復後の push が起動していた) | canon を訂正し、`@codex review` を出す設計に変えた |

## 残っていること

- codex-limits.sh は Claude の sandbox 内では app-server の HTTP が proxy を越えられず失敗する (canon に記録)。reset が分かる経路は fake でしか動かしていない。次の実際の上限で実測する。
- reviewer を worktree 隔離で起動すると、この checkout の `core.hooksPath` が絶対パスに書き換わることが 1 回観測された (21:29、4 体の起動と同時刻)。verify.sh が sandbox 内で直せず落ちる。原因は未確定。
- Codex の指摘は findings.md に書く手順になったが、今回の 19 ラウンド分は書いていない。
