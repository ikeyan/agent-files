ランタイム・標準ライブラリ・既存の依存で実装済みの機能を、それらを見ずに自前で実装していないか。

検出手順: diff で新規に書かれたユーティリティ (パース、比較、探索、待ち、タイムアウト、リトライ、パス操作、住所・電話番号などのドメイン固有値の処理) を列挙し、それぞれ「ランタイム標準・既存の依存・同じリポジトリ内に同じ機能が無いか」を名前で検索して答える。あれば、それを使う案を出す。依存を足せないなら、既存の実装から必要な機能とテストケースを抽出して再実装し、1 つのモジュールにまとめる案を出す。
匂い: 自前の semver 解析、自前の `sleep` / タイムアウト / `Promise` ラッパ、自前のパス正規化、同じリポジトリに似た名前の関数 (find*/resolve*/detect*) が既にある。
