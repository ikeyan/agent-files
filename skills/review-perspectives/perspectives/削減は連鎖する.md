1 行・1 引数・1 分岐を削った変更で、それに依存していた変数・関数・引数がまるごと不要になっていないか。

検出手順: diff で削除された行・引数・分岐を全て列挙し、各々が参照していた変数・関数・引数・import を書き出す。そのうち残りのコードから参照されなくなったもの、1 か所からしか使われず 1 段浅くできるものを指摘し、削る案を出す。
匂い: 削除された分岐の直前に残った変数の代入, 呼び出し元が 1 つになった関数, 常に同じ値で呼ばれるようになった引数, 使われなくなった import
