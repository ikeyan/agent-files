同じ値・式・処理の繰り返しを、短い 1 つの名前にまとめられないか。

検出手順: diff で追加された値・式・処理の塊のうち、同じもの (または引数だけ違うもの) がファイル内・diff 内に 2 回以上現れるものを全て列挙する。各々に短い名前を付けられるなら、定数・関数・変数への共通化を案として出す。
匂い: 同じリテラルの繰り返し, コピーして一部だけ変えたブロック, 同じ URL・パス・コマンド行の繰り返し
