TypeScript の型キャスト `as` が、消すと型エラーになるものだけになっているか。

検出手順: diff 内の `as` を全て列挙する。`as unknown as T` の内側にさらに `as` がある場合、片方は不要。各 `as` について「消したら型エラーになるか」を答え、ならないものは削除候補。`globalThis as unknown as` は `declare global` にする案を出す。
匂い: `as unknown as`、`as never`、`as any`、テストコード内の `as`。
