冪等な操作に、結果を変えない存在確認のガードが付いていないか。

検出手順: diff で追加された条件分岐のうち、分岐の中身が集合への追加・削除、map への代入、ファイルの上書き、mkdir -p のような冪等な操作だけのものを全て列挙する。条件を外しても結果が同じなら、分岐を外した案を出す。
匂い: `if k not in s: s.add(k)`, `if (!map.has(k)) map.set(k, v)` で v が常に同じ, `[ -d x ] || mkdir -p x`, `if exists: delete`
