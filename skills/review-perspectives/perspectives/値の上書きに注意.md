同じ Map の同じ key や同じ `let` への代入が複数回起きうるとき、古い値が上書きされて問題ないか。

検出手順: 同じ変数・同じ Map key への代入が複数箇所にある場合を列挙し、後勝ちで良いかを答える。
匂い: 同じ Map への `set` が複数箇所、同じ `let` への代入が分岐の両側、`Object.assign` / spread による上書き、ループ内で外側の変数へ代入。
