set / map のキーに複数要素の組を使うとき、要素間の関数従属で次元を落とせないか。

検出手順: diff で追加・変更された set / map / 辞書 / キャッシュのキーのうち、組・連結文字列・複数フィールドのものを全て列挙する。各要素について、他の要素から一意に決まるか (`(host, port)` が `user` から決まる等) を書き出し、決まる要素を除いたキーを案として出す。
匂い: タプルのキー, f-string や join で連結したキー, 複数フィールドを持つキーの型
