新設・変更された設定を選ぶ根拠を、ユーザーが持てるか。

検出手順: 新設・変更された設定項目・preset・オプションを列挙し、「ユーザーはこれを選ぶ根拠を持てるか、それとも実装や環境が決めることか」を答える。
匂い: 環境 (インストール済みの版・OS・存在するファイル) で一意に決まる値を設定にしている、設定値の説明が「内部で〜する」で始まる、似た名前の preset / mode が 2 つあり違いが実装詳細。
