失敗を、呼び出し側が分類し忘れうる戻り値 (null / undefined / false) で表していないか。

検出手順: null / undefined / false を返す関数を列挙し、その値が「正常な結果」か「失敗」かを分類する。失敗なら例外にする案を出す。
匂い: `return undefined` / `return null` / `return false` が catch 節にある、タイムアウトを `null` で表す、呼び出し側で `if (!x) return` が連鎖している、`?? デフォルト値` で失敗を隠している。
