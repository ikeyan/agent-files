外部入力の型と形式の検査が、入力を読んだ直後に全体に対して行われているか。

検出手順: 外部入力 (JSON・引数・環境変数) を読む箇所を列挙し、読んだ直後に形を検査しているか、利用側に防御コードが散っていないかを見る。
匂い: `JSON.parse` の結果を検査せず `.` でたどる、`process.argv` / `process.env` を複数の関数で直接読む、関数の入口以外にある `if (typeof x !== ...)`、相対パスをそのまま `join` / `dirname` に渡す (入口で絶対パスに正規化していない)。
